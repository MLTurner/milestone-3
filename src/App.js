import React from 'react';
import { Button } from '@mui/material';

const App = () => {
  return (
    <div>
      <h1>Hello, World!</h1>
      <Button variant="text">test</Button>
    </div>
  );
};

export default App;
