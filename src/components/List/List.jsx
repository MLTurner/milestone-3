import React from "react";
import { CircularProgress, Grid, Menu, MenuItem, Typography, InputLabel, FormControl, Select } from "@mui/material"

import useStyles from './styles'
const List = () => {
    return(
        <h1>List</h1>
    );
}
export default List;